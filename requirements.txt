# First, uninstall numpy to remove any pre-installed conflicting versions
!pip uninstall -y numpy

# Then install the correct version of numpy
numpy==1.24.3

# Install TensorFlow without automatically installing numpy
tensorflow==2.14.1 --no-deps

# Install the rest of the dependencies
pandas==2.0.2
joblib==1.2.0
matplotlib==3.7.1
seaborn==0.12.2
plotly==5.12.0
streamlit==1.10.0
scikit-learn==1.2.2
streamlit-option-menu==0.3.2
pyarrow==12.0.1
fastparquet==0.9.0
